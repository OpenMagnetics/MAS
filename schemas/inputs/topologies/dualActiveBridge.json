{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "http://openmagnetics.com/schemas/inputs/topologies/dualActiveBridge.json",
    "title": "dualActiveBridge",
    "description": "The description of a Dual Active Bridge (DAB) converter excitation",
    "type": "object",
    "properties": {
        "inputVoltage": {
            "description": "The input voltage of the DAB converter",
            "$ref": "../../utils.json#/$defs/dimensionWithTolerance"
        },
        "efficiency": {
            "description": "The target efficiency",
            "type": "number",
            "default": 0.97
        },
        "seriesInductance": {
            "description": "The series inductance for power transfer control. If 0, uses transformer leakage inductance",
            "type": "number",
            "default": 0
        },
        "useLeakageInductance": {
            "description": "Whether to use transformer leakage inductance as the series inductor",
            "type": "boolean",
            "default": true
        },
        "operatingPoints": {
            "description": "A list of operating points",
            "type": "array",
            "items": {
                "$ref": "#/$defs/dabOperatingPoint"
            },
            "minItems": 1
        }
    },
    "required": ["inputVoltage", "operatingPoints"],

    "$defs": {
        "dabOperatingPoint": {
            "description": "The description of one DAB operating point",
            "title": "dabOperatingPoint",
            "type": "object",
            "properties": {
                "outputVoltages": {
                    "description": "A list of output voltages, one per output",
                    "type": "array",
                    "items": {
                        "type": "number"
                    },
                    "minItems": 1
                },
                "outputCurrents": {
                    "description": "A list of output currents, one per output",
                    "type": "array",
                    "items": {
                        "type": "number"
                    },
                    "minItems": 1
                },
                "phaseShift": {
                    "description": "The phase shift at this operating point in degrees",
                    "type": "number"
                },
                "switchingFrequency": {
                    "description": "The switching frequency of the operating point",
                    "type": "number"
                },
                "ambientTemperature": {
                    "description": "The ambient temperature of the operating point",
                    "type": "number"
                },
                "modulationType": {
                    "description": "DAB modulation type: SPS (Single Phase Shift, default), EPS (Extended Phase Shift, D1>0 on primary only), DPS (Dual Phase Shift, D1=D2), TPS (Triple Phase Shift, D1 and D2 independent)",
                    "type": "string",
                    "enum": ["SPS", "EPS", "DPS", "TPS"],
                    "default": "SPS"
                },
                "innerPhaseShift1": {
                    "description": "Primary bridge inner phase shift in degrees (D1). 0 = square wave. Used for EPS, DPS, TPS.",
                    "type": "number"
                },
                "innerPhaseShift2": {
                    "description": "Secondary bridge inner phase shift in degrees (D2). Used for TPS. For DPS, if absent, equals innerPhaseShift1.",
                    "type": "number"
                }
            },
            "required": ["outputVoltages", "outputCurrents", "switchingFrequency", "phaseShift", "ambientTemperature"]
        }
    }
}
